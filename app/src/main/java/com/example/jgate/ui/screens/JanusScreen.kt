package com.example.jgate.ui.screens

/**
 * Defines all nav routes within JGate
 * Each enum constant is a screen that the user can navigate to, the .name
 * property is used as the route string in the NavHost
 */

enum class JanusScreen {
    Login,
    CredentialList,
    AddCredential,
    EditCredential,
    CredentialDetail,
    PasswordGenerator
}